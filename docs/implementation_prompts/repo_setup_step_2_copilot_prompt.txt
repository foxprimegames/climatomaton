please create a new branch with an appropriate name for what step 2 is accomplishing, then proceed with step 2.

>>>>

Starting Step 2: Create Developer Guide and developer tooling files. I will add docs/DEVELOPER_GUIDE.md, dev-requirements.txt, pyproject.toml, and scripts/lint.ps1 to the repository. I will then mark the step completed.

(lots of excess files created and the command to commit them offered)

----

you failed to understand what step 2 is.  step 2 was to create the developer guide, not all of those other files.

>>>>

You’re right — I added extra files. I’ll fix that.
Plan (short)
•	Create branch docs/developer-guide and commit only docs/DEVELOPER_GUIDE.md.
•	Leave dev-requirements.txt, pyproject.toml, and scripts/lint.ps1 uncommitted in the working tree so they can be addressed in the appropriate step.
If that sounds good, reply “proceed” and I will run these commands in the repo root:
•	git checkout -b docs/developer-guide
•	git add docs/DEVELOPER_GUIDE.md
•	git commit -m "docs: add developer guide"
•	git show --name-only --pretty=format:%B HEAD
If you prefer a different action (stash the other files, commit them to a separate branch, or remove them), tell me which and I’ll proceed.

----

remove the extra files to keep the repo clean.

>>>>

I removed the three extra files you asked (dev-requirements.txt, pyproject.toml, scripts/lint.ps1) from the workspace.
Please run these commands in the repo root to create the branch for the Developer Guide and commit only docs/DEVELOPER_GUIDE.md:
•	git checkout -b docs/developer-guide
•	git add docs/DEVELOPER_GUIDE.md
•	git commit -m "docs: add developer guide"
•	git show --name-only --pretty=format:%B HEAD
Tell me when you are done and I will proceed to the next step.

----

if the developers' guide is not going to be in the root directory, it does not need to be in all caps.

>>>>

Agreed — use a lowercase, hyphenated name for consistency (docs/developer-guide.md). I can rename the file and commit the change.
Run these commands to apply the rename and commit:
•	git mv docs/DEVELOPER_GUIDE.md docs/developer-guide.md
•	git commit -m "docs: normalize developer guide filename to docs/developer-guide.md"
Or tell me “proceed” and I will run them for you.

----

you still need to commit into a branch specific for this step.

>>>>

(tried to use a `git mv` command on a file that was not yet committed, which caused an error; then it offered a powershell script to do the rename and then commit it)

----

(Saved the project files which I'd fogotten to do)

please amend the commit with the updated project and solution files

>>>>

Amended commit to include climatomaton.pyproj. The commit now contains docs/developer-guide.md and climatomaton.pyproj.
Shall I push docs/developer-guide branch to origin?

----

yes

>>>>

Branch docs/developer-guide pushed to origin successfully.
Next: proceed to Step 3 (uv workspace init) or pause?

----

please amend the commit to reference https://codeberg.org/foxprimegames/climatomaton/issues/25 (on a separate line from the summary) and forcibly push it

>>>>

Amended the commit message to include the issue reference and force-pushed the docs/developer-guide branch to origin.
Next: proceed to Step 3 (uv workspace init) or pause?

----

please switch to main, pull and prune, and remove the deleted branch locally

>>>>

Done: switched to main, pulled with fast-forward, pruned remote branches, and removed the local docs/developer-guide branch.
Anything else to proceed with?

----------------------------------------------------------------

for each step from now on:
1. when starting the step, create and switch to a new branch with an appropriate name for what that step will accomplish.
2. only perform the tasks included in that step.
3. after the step is implemented, remind me to "save all" so that the project and solution file updates get saved.
4. do not commit any changes without a reference to a codeberg issue number.
5. always place the reference to the issue number on a separate line from the summary.

please repeat what you understand from those instructions.

>>>>

I understand and will follow these rules for every remaining step:
1.	Create and switch to a new branch named for the step before making changes.
2.	Only perform the tasks defined for that single step on that branch.
3.	After implementing the step, remind you to “Save All” so solution/project updates are written to disk.
4.	I will not create any commit unless the commit message includes a Codeberg issue reference.
5.	The issue reference will be on its own separate line in the commit message (separate paragraph from the summary).
I will also pause before committing and require you to provide the Codeberg issue number to include.
