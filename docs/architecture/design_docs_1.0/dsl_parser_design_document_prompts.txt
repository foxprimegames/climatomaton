----

based on the attached architecture file and design document list, let's start working on the dsl parser design.

--

1. let's include the EBNF for the language here, since we're going to be making updates to it; include a note to that effect.
2. add a recursive `Expression` into the `Relational` definition
3. add syntax for a function call into both `Relational` and `ArithExpr`.  functions are identified by a `NamespacePath`.
4. whether in this document or elsewhere, we need to identify built-in functions and functions to be included on the native `climate` and `proposals` environment objects.
5. "climate rule" and "tag rule" should each be two tokens, not single tokens, to make it easier to add other types of rules also.
6. to make it more clear that an `Expression` is a conditional or logical expression, please rename it appropriately.
7. function call arguments need to support either conditional expressions or arithmetic expressions.

--

1. Please allow function call arguments to also be a literal string.
2. Move the EBNF to section 1, before discussing tokenization.

--

the description of the updated ebnf does not need to indicate "literal string arguments", since that's part of the addition of function calls.

--

instead of describing a bunch of different types of tokens in section 2, after the section introduction, provide a table of token names and their description (or literal text match).

--

1. The definition of `TOKEN_STRING` needs to define how double-quotes and special characters (including unicode characters) can be included in the string.
2. in the AST logic, you _don't_ need as much specificity.  the rules you list come directly from the EBNF grammar.  What you need to do is to define what the AST _is_.
3. if `climate.hasTag()` is a function, then we don't need the `contains` operator in the EBNF.
4. we need to figure out how to deal with `climate.value`.  in conditionals, that's almost always going to be the value from before the batch execution of rules, but in actions, it's generally going to refer to the temporary state being processed.  please bring this up for discussion after applying other changes.
5. `proposals.getPassRatio()` is not needed, since that's exactly `proposals.passed / proposals.count`.
6. `min()` and `max()` should take any number of arguments.
7. `round(0` needs to specify how values at exact 0.5 fractions are rounded, both in positive and negative values.

--

in section 3, `ArrayMutationNode` should be called `SetMutationNode`.  Also, you don't mention arithmetic nodes or function call nodes.

--

is `VariableReferenceNode` the same as a NamespacePath reference?  if so, it should be a `NamespacePathNode`, and the same AST node should be referenced from the function call node.

in regards to `climate.value`, requiring `climate.getPreviousValue()` in the conditional section is a recipe for user error, since conditionals _always_ want the previous value and it would be easy to forget when starting a new rules file.

what if we define that, within the conditionals block, all `NamespacePath` references refer to the environment as it existed before the current set of rules (meaning climate rules vs tag rules) were processed, while within the actions block, all `NamespacePath` references refer to the environment as it is being mutated by the rules?

--

how does section 5 work if most of the rules parsing/handling logic does not know the physical location of any of the rules?

--

(need to continue after token limit resets)

you misunderstood the question.  most of the rules parsing and handling logic does not know which file contained a rule or where that rule was within the file.  how do the rules get their IDs properly assigned when the parser logic is only receiving a stream of stateless tokens?

--

why does the token metadata need to include the filenumber if that information is also in the compilation context?

--

in that case, update the compilation context to also include the file _name_, since the file number is not enough to uniquely identify the file.

--

for the stateful ast id allocation, i assume "requests a new id" means requesting a new id of the appropriate type.

--

good.  please re-display the entire document with no conversation before or afterwards.

----

(after restarting on a new session)

based on the attached architecture file, design document list, and first draft of the dsl parser design, let's continue working on it.

for the NamespacePath solution in section 7, it would be useful to create an `initial` environment object that contains the entire execution environment from the start of the rules processing; this way, even if we do the scope-change between `when` and `then` blocks, the `then` blocks still have access to the initial state if they wish.

for example:
```
climate rule "if all proposals failed, double the climate change for this turn"
when proposals.passed == 0 and proposals.failed > 0
then climate.value += climate.value - initial.climate.value
end
```

that `initial` environment object needs to be read-only, which means we need a way for the parser to recognize a read-only NamespacePath and reject assignments to that path or any child of that path.  in other words, the following example needs to fail:

```
climate rule "if all proposals failed, double the climate change for this turn"
when proposals.passed == 0 and proposals.failed > 0
then initial.climate.value += climate.value - initial.climate.value
end
```

since we will have such a mechanism, we'll want to be able to have PEMs dictate their own read-only NamespacePaths for their environment objects.  the way that PEMs pass that information to the core engine will be defined at a later time, but this document needs to define the protocol by which the core engine tells the parser which NamespacePath prefixes are read-only.

--

PEMs cannot "dynamically" request locks, at least not in the way "dynamically" is usually meant.  PEMs do not directly communicate with the core engine.  This section will have to define _how_ PEMs communicate their read-only sub-paths to the core engine.  Please pay attention to the attached shared volume contract to ensure you don't negatively impact any of the other protocols already in place.

--

please include an example of a PEM marking a child of one of its properties as read-only.

--

good.  now we need to present two separate documents, with no extraneous conversation.  i'll ask for the first, and then ask for the second in the subsequent prompt.


first: the revised shared volume contract document that includes the documentation of the read-only protocol described above used by PEMs.

--

okay, back to conversation mode.  i need to roll back a bunch of this conversation.  i just realized that the climate rules should never be able to modify any environment object other than `climate`.  therefore, instead of having an `initial` read-only namespace, there should be a `new` mutable namespace that _only_ includes the `climate` object.

--

one correction.  the parser checks if the left hand target string begins with `new.`.  there might be other mutable namespaces introduced in the future.

--

please remove the comment about the contains operator being replaced.

--

1. please expand the "standard escape sequences".  make sure that it's specified that unicode escape sequences are also accepted.
2. please define "rounds half away from zero".
3. please replace "sorting engine" with "parsing engine".
4. when referring to the `.json` files, call them "environment objects" not "states".

----

so, for the update to the DSL design document, what would you suggest for a git commit, following best practices for git commit messages?

--

okay, i made an assumption that i shouldn't have, which is that you would have known i didn't update the DSL document with the interim update that included the read-only environment tree.


please create a git commit message for the latest version of the dsl document against the version that i uploaded at the start of the conversation.

--

the previous architecture did not rely on manual metadata declarations, because everything was mutable.

--

please include the fact that the `new.` prefix currently only includes the `climate` object, although other objects may be added to it in the future.


Also, you can use (minimal) markdown formatting for the git commit message (such as `new.` instead of 'new.')

--

now rework the message to follow best practices for git commit messages.  (your subject is too long)
