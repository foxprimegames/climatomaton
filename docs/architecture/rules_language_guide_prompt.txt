----

Based on the uploaded architecture document (and the prompts that led to it and its updates), the uploaded rules intermediate representation design document (and the prompts that led to that document), the uploaded pem design document (and the prompts that led to it), and the uploaded pending document updates list, we need to create a language guide and reference for the rules language.

Please choose a rules language that is easy for a non-technical, non-programmer to understand and to write.  Choose grammar and syntax that reduces "typo" errors.  for the language guide, use non-technical language to explain how to write the rules and what they mean.  for the language reference, use the technical language and include the detailed specifications of the language.

as always, please process the above issues into both documents and display them, guide first followed by reference.  remember to use code segments (backticks) instead of latex markup, and any time you want to include a double-asterisk as a stand-alone code segment, use two code segments with a single asterisk each with a space between the two code segments.  place any comments, new issues, other discussion points, or questions after the end of the document.  please also include the full list of updates needed to any other documents that were mentioned in this prompt or response or any previous prompt or response; place that list of updates after any comments, new issues, discussion points, and questions.  finally, please avoid any trailing spaces in your output.

--

1. in the guide section 3, for the external namespaces, instead talk about "future additions to the system might introduce new namespaces (e.g. `weather.`)"; don't talk about modules and PEMs.
2. in the guide section 4, should tag lists maybe not use the square brackets?  i understand that can cause a conflict between a single string and a tag list with a single tag, but can that be resolved without using the bracket syntax?
3. in the guide section 5, in the math comparisons, please include `< .. <` or `<= .. <=` in the comparisons.  these can be defined in the language reference as syntactic sugar for the `within` function.
   * because you're providing a different way of doing `within`, use a different function for the example, such as `climate.tags.has("Mild")`
4. for guide section 6, would it read easier if you say `<target> is <value>`, `<target> is increased by <value>`, `<target> is decreased by <value>`, along with your existing tag list syntax of `<target> includes <tags>` and `<target> excludes <tags>`?

5. for the reference, section 3 needs to specify the syntactic sugar of `x <(=?) N <(=?) y` is the same as `N.within(x, y, bounds)`
6. in the reference function lists, please include both function and method syntax (`has(list, tag)` and `list.has(tag)`)
7.

as always, please process the above issues into both documents and display them, guide first followed by reference.  remember to use code segments (backticks) instead of latex markup, and any time you want to include a double-asterisk as a stand-alone code segment, use two code segments with a single asterisk each with a space between the two code segments.  place any comments, new issues, other discussion points, or questions after the end of the document.  please also include the full list of updates needed to any other documents that were mentioned in this prompt or response or any previous prompt or response; place that list of updates after any comments, new issues, discussion points, and questions.  finally, please avoid any trailing spaces in your output.

--

1. again, DO NOT USE the term `mutable`.
2. ensure there is a way to specify that a single string is actually supposed to be a tag list (e.g. adding a comma at the end or something).  the syntax _does not always_ dictate the context (`is` could be assigning to a namespace path that is a string type or a namespace path that is a tag list type, and the parser won't necessarily know what type that namespace path is; it still needs to provide the correct type in the intermediate representation)
3. include the EBNF for the language in the reference section.

as always, please process the above issues into both documents and display them, guide first followed by reference.  remember to use code segments (backticks) instead of latex markup, and any time you want to include a double-asterisk as a stand-alone code segment, use two code segments with a single asterisk each with a space between the two code segments.  place any comments, new issues, other discussion points, or questions after the end of the document.  please also include the full list of updates needed to any other documents that were mentioned in this prompt or response or any previous prompt or response; place that list of updates after any comments, new issues, discussion points, and questions.  finally, please avoid any trailing spaces in your output.

--

1. in the guide section 4:
   * how about the keyword `empty` for an empty list, rather than `[]`?
   * "only exactly" is not proper usage.
2. in the guide section 5, would it be confusing to say that, in the conditions list, you can give multiple conditions _without_ the `and` in between?  (if allowed, this should definitely be specified in the reference, but should it be in the guide?)
3. in the guide section 6, specify that booleans and strings can only use `<target> is <value>`.
4. for a non-programmer, the syntax in the guide section 6 might indicate that you would use, e.g. `<new.climate.value> is <10>`.  maybe place the variable parts in italics and the keywords in code? (e.g. _target_ `is` _value_, _target_ `is increased by` _value_, etc.)
5. the intermediate representation allows for multiple conditions, but the ebnf you give only allows a single condition.  was this an intentional choice, and if so, what were the reasons behind it?
6. in the ebnf, the comparison syntax is incorrect. the first alternative is just `Arithmetic CompOp Arithmetic`, with no 0-or-more syntax allowed.
7. in the ebnf, mod should end the term and not allow another term operator after it.  i'm tempted to want to split that up into four rules, one for each operator and one for none (e.g. `Term ::= Factor | Factor ("*" Factor)+ | Factor ("/" Factor)+ | Factor "mod" Factor`)
8. the string literal syntax does not allow for backslash-escaped quotes or backslashes.
9. any way to make the string literal EBNF not mess up the way the renderer identifies quoted strings and treat the rest of the syntax as all part of an unterminated string?
10. should we make another "syntactic sugar" to allow the `climate.tags includes` (string-or-tag-list)` to translate to `climate.tags.has(string-or-tag-list)` and `climate.tags includes (string-or-tag-list)` to translate to `not climate.tags.has(string-or-tag-list)`?
11. in the reference, you don't need to explain what EBNF means.
12. you only should have replaced mutable in the language guide.  i was .. too enthusiastic with my prohibition.  at least in the pending updates, please put the language back to "mutable".

as always, please process the above issues into both documents and display them, guide first followed by reference.  remember to use code segments (backticks) instead of latex markup, and any time you want to include a double-asterisk as a stand-alone code segment, use two code segments with a single asterisk each with a space between the two code segments.  place any comments, new issues, other discussion points, or questions after the end of the document.  please also include the full list of updates needed to any other documents that were mentioned in this prompt or response or any previous prompt or response; place that list of updates after any comments, new issues, discussion points, and questions.  finally, please avoid any trailing spaces in your output.

--

1. okay, to be specific: in the language guide, use either "modifyable" or "changeable" instead of the word "mutable".  you can use "mutable" in the reference guide , the discussion section, and the pending updates.
2. to address the `includes`/`excludes` -> either `has_all` or `has_any`, more keywords!  `<source> includes <value>` -> `has(<source>, <value>)`; `<source> includes any of <value>` -> `has_any(<source>, <value>)`; `<source> includes all of <value>` -> `has_all(<source>, <value>)`. `excludes` is the same but prefixed by `not`.
3. in the ebnf, to make it clear that the range comparison is syntactic sugar, move its definition into `ConditionSugar` as well.
4. splitting the arithmetic term up to only support chained multiplications or chained divisions but not mixed was my thought.  do you think it's correct?  it's probably not expected is my worry now.  i do still want `mod` to not chain.
5. the attempt to fix the ebnf string rendering issue was valiant, but unsuccessful.  go ahead and put it back to the more compact form.  maybe stick a comment at the end with what the renderer thinks is the unmatched quote.
6. the example made me realize we need a unary negative.  please add that to the language guide and reference, and add a pending update to the rules intermediate representation design doc to add the unary negative to the json-ir format.
7. we should include syntactic sugar for the actions to (a) allow either `include` or `includes` and `exclude` or `excludes` for those action types, and (b) allow `xxx includes yyy and excludes zzz` or `xxx excludes yyy and includes zzz` syntax.  both of the `(b)` formats would be rendered into a pair of json-ir action nodes.
8. once again, the pem design document is already written and includes the update you provided.
9. let's take the example out of the guide section 6, and create a new guide section 7 as "Example set of rules".  include the following rules (formatted properly) with descriptions of what they do:

```
climate rule "not enough activity" when proposals.count < 5 then new.climate.value is decreased by 5 - proposals.count
climate rule "disagreements cause heated discussions" when proposals.count >= 5 and proposals.failed > proposals.passed then new.climate.value is increased by proposals.failed - proposals.passed
climate rule "agreement makes everything calm down (warm climate cooling)" when proposals.count >= 5 and proposals.passed > proposals.failed and new.climate.value > proposals.passed - proposals.failed then new.climate.value is decreased by proposals.passed - proposals.failed
climate rule "agreement makes everything calm down (warm climate becomes neutral)" when proposals.count >= 5 and proposals.passed > proposals.failed and new.climate.value <= proposals.passed - proposals.failed then new.climate.value is 0
climate rule "agreement makes everything calm down (cool climate warming)" when proposals.count >= 5 and proposals.passed > proposals.failed and new.climate.value < -(proposals.passed - proposals.failed) then new.climate.value is increased by proposals.passed - proposals.failed
climate rule "agreement makes everything calm down (cool climate becomes neutral)" when proposals.count >= 5 and proposals.passed > proposals.failed and new.climate.value >= -(proposals.passed - proposals.failed) then new.climate.value is 0

tag rule "hope it stays mild" when -10 <= new.climate.value <= 10 then new.climate.tags include "mild" and exclude "greenhouse", "ice age"
tag rule "brrrr" when new.climate.value < -10 then new.climate.tags include "ice age" and exclude "mild", "greenhouse"
tag rule "hothothot" when new.climate.value > 10 then new.climate.tags include "greenhouse" and exclude "ice age", "mild"
tag rule "getting warmer" when new.climate.value > climate.value then new.climate.tags include "warming" and exclude "cooling"
tag rule "getting cooler" when new.climate.value < climate.value then new.climate.tags include "cooling" and exclude "warming"
tag rule "not changing" when new.climate.value = climate.value then new.climate.tags exclude "cooling", "warming"
```

as always, please process the above issues into both documents and display them, guide first followed by reference.  remember to use code segments (backticks) instead of latex markup, and any time you want to include a double-asterisk as a stand-alone code segment, use two code segments with a single asterisk each with a space between the two code segments.  place any comments, new issues, other discussion points, or questions after the end of the document.  please also include the full list of updates needed to any other documents that were mentioned in this prompt or response or any previous prompt or response; place that list of updates after any comments, new issues, discussion points, and questions.  finally, please avoid any trailing spaces in your output.

--

1. Please add a syntax for comments.  i was originally going to suggest using either `#` or `//` or both for through-end-of-line comments, but then thought it might be more natural to say that comments are enclosed in square brackets (e.g. `when proposals.count < 5 [ not a lot of activity ]`).  comments must be allowed anywhere (_except possibly in between words of a multi-word keyword_) and are explicitly ignored.  note, the last sentence was a directive to you, not language that should appear in the guide; please explain comments appropriately in the guide.
2. In the (current) section 7 rule set example, please add comments with your understanding of what each rule or group of rules is doing.
3. you added a comment outside the code block for the ebnf, which doesn't do anything (and also you targeted the wrong quote).  the comment should be an inline comment at the end of the EBNF for StringLiteral; or better yet, to make sure it works properly, StringLiteral should be broken up into two lines, each of which ends in an inline comment:
```
StringLiteral ::= '"' [^"\\]* '"' # keep rendering engines happy: "
                | "'" [^'\\]* "'" # keep rendering engines happy: '
```
4. question: would introducing conditional expressions (such as C's ternary operator or python's `x if y else z` syntax) create more confusion than it might solve?  such expressions would have allowed the example set of "agreement makes everything calm down" rules into two rules rather than 4.
5. in the guide "writing actions" section, choose a different way to explain why there are different specific keywords to change data.  also, for the "more compact" explanation, say tag list modifications instead of just tag modifications.
6. in the reference section on natural-language constructs, "action items combined via `and`" should specifically talk about tag-list actions.
7. somewhere, probably in the "environments" section, the guide needs to discuss the structure of the `var` namespace.
8. in the reference section on action mutations, the explanation of the engine not being able to statically block certain mutations is not accurate, since the syntax could preclude that, but that would preclude the use of tag list variables also.
9. in the "types of data" section of the guide, don't include the comment about making a number negative by placing a - sign in front of it, since you already have an example that shows that.
10. in the guide on types of data and operations, the `includes`/`excludes` tag list operators allow checking for a **single** tag inside the target tag list, whereas the `any of` and `all of` tag list operators are the two different ways of checking if one or all tags from a **tag list** are inside the target tag list.  the previous sentence was a directive to you; please explain it appropriately within the guide.
11. in the example for the actions for a tag list, please use the singular verbs `includes` and `excludes`, since the example *target* is a singular word.
12. in the ebnf for `Factor`, only allow one optional `^` operator, not a chain of them.

as always, please process the above issues into both documents and display them, guide first followed by reference.  remember to use code segments (backticks) instead of latex markup, and any time you want to include a double-asterisk as a stand-alone code segment, use two code segments with a single asterisk each with a space between the two code segments.  place any comments, new issues, other discussion points, or questions after the end of the document.  please also include the full list of updates needed to any other documents that were mentioned in this prompt or response or any previous prompt or response; place that list of updates after any comments, new issues, discussion points, and questions.  finally, please avoid any trailing spaces in your output.
