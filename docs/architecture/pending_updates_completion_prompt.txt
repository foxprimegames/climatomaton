----

(Upload in order:  architecture.md, rules_intermediate_representation_design_doc.md, rules_language_guide.md, rules_language_reference.md, git_fetch_prm_design_doc.md, pem_design_doc.md, and pending_updates.md)

I have uploaded the architecture document, existing design documents, and the current pending document update list for the Nomicron climate system project.

The document we are working on now is the pending document update list itself.

Let's complete existing entries or add new entries into the pending document update list for every design document that needs to be written.

The documents should be listed in reverse chronological order from the way order they should be completed, so that, as documents are removed from the list, other documents do not have to be renumbered.  Please include a note to that effect in the header of the document.

The first priority to complete should be the overall architecture document.

The last items to complete should be the codebase document and the deployment document, respectively.

process the above instructions into the pending updates list and render it.  ONLY make changes to the pending updates list that were discussed in the instructions.  always place any comments, new issues, other discussion points, or questions after the end of the pending updates list.  remember to use code segments (backticks) instead of latex markup, and any time you want to include a double-asterisk as a stand-alone code segment, use two code segments with a single asterisk each with a space between the two code segments.  avoid any trailing spaces in your output.
